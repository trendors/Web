import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trendors.app',
  appName: 'trendors',
  webDir: 'dist/trendors-web-frontend/browser'
};

export default config;
